FROM golang:1.26-alpine

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

CMD sh -c 'go test ./... -v -short -cover -coverprofile=coverage.out -count=1 && echo "" && echo "=== TOTAL COVERAGE ===" && go tool cover -func=coverage.out | tail -1'
